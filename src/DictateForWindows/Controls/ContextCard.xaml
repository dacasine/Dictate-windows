<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="DictateForWindows.Controls.ContextCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DictateForWindows.Controls"
    Width="150"
    Height="100">

    <Border
        x:Name="CardBorder"
        Background="{StaticResource ContextPanelBackgroundBrush}"
        BorderThickness="1.5"
        CornerRadius="10"
        Padding="10,8"
        PointerPressed="OnCardPointerPressed">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header with icon -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="6">
                <FontIcon
                    x:Name="CardIcon"
                    FontFamily="Segoe Fluent Icons"
                    FontSize="14"
                    Foreground="{StaticResource OrbTextSecondaryBrush}"/>
                <TextBlock
                    x:Name="HeaderText"
                    Style="{StaticResource OrbSecondaryTextStyle}"
                    FontSize="11"/>
            </StackPanel>

            <!-- Preview text -->
            <TextBlock
                Grid.Row="1"
                x:Name="PreviewText"
                Style="{StaticResource OrbPrimaryTextStyle}"
                FontSize="11"
                TextTrimming="CharacterEllipsis"
                MaxLines="2"
                TextWrapping="Wrap"
                Margin="0,4,0,0"/>

            <!-- Loading indicator -->
            <ProgressRing
                Grid.Row="2"
                x:Name="LoadingRing"
                IsActive="False"
                Width="20"
                Height="20"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"/>
        </Grid>
    </Border>

</UserControl>

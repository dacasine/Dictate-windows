<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="DictateForWindows.Controls.ModelLens"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DictateForWindows.Controls">

    <Button
        x:Name="LensButton"
        Style="{StaticResource PromptLensButtonStyle}"
        Click="OnLensClick"
        ToolTipService.ToolTip="{x:Bind ModelName, Mode=OneWay}">
        <TextBlock
            x:Name="LensText"
            Text="{x:Bind ModelName, Mode=OneWay}"
            FontFamily="Segoe UI Variable"
            FontWeight="Light"
            FontSize="11"
            CharacterSpacing="20"/>
    </Button>

</UserControl>
